import { Typography, Container } from "@mui/material";

const Skills = () => {
  return (
    <Container>
      <Typography variant="h3">Skills Page</Typography>
    </Container>
  );
};

export default Skills;