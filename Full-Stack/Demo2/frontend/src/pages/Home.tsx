import { Typography, Container } from "@mui/material";

const Home = () => {
  return (
    <Container sx={{bgcolor: "red",p: 0,m: 0,minWidth: "100%"}}>
      <Typography variant="h3">Home Page</Typography>
    </Container>
  );
};

export default Home;